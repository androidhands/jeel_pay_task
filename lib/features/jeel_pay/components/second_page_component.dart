import 'package:flutter/material.dart';
    
class SecondPageComponent extends StatelessWidget {

  const SecondPageComponent({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}